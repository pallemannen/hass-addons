configuration:
  target_host:
    name: Destination host
    description: IP or hostname to forward all traffic to
  target_port:
    name: Destination port
    description: Port to forward all traffic to
